services:
  server:
    build:
      context: .
      dockerfile: docker/server.Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - apps/server/.env
    environment:
      NODE_ENV: production
      PORT: 3000
      PUBLIC_DIR: /app/public
    volumes:
      - ./public:/app/public:ro
